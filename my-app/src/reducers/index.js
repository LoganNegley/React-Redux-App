
const initialState = {
    isLoading: false,
    pokemon: null,
    error: ''

}


export function pokemonReducer(state = initialState, action){
    switch(action.type){
        default: 
        return state;
    }
}